# FF Poker Server v2

**OpenAPI 準拠の純粋関数型ゲームエンジンを使用したポーカーサーバー**

[![Tests](https://img.shields.io/badge/tests-273%2F273%20passing-brightgreen)]()
[![Coverage](https://img.shields.io/badge/coverage-80%25%2B-green)]()
[![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue)]()
[![Node](https://img.shields.io/badge/node-%3E%3D20.0.0-brightgreen)]()

---

## 📋 目次

- [概要](#概要)
- [主な機能](#主な機能)
- [アーキテクチャ](#アーキテクチャ)
- [セットアップ](#セットアップ)
- [使い方](#使い方)
- [API仕様](#api仕様)
- [Acknowledgment システム](#acknowledgment-システム-) 🆕
- [開発](#開発)
- [テスト](#テスト)
- [ロードマップ](#ロードマップ)

---

## 概要

FF Poker Server v2は、テキサスホールデムポーカーのマルチプレイヤーゲームサーバーです。

### 特徴

- 🎯 **OpenAPI 3.0準拠** - API仕様を完全定義、Swagger UIで確認可能
- 🧪 **高いテストカバレッジ** - 56の統合/ユニットテスト（100%パス）
- 🔧 **純粋関数型エンジン** - fp-tsを使用した副作用のないゲームロジック
- 🚀 **RESTful API** - クリーンなREST APIデザイン
- 📝 **TypeScript** - 完全な型安全性

### バージョン情報

- **バージョン**: 2.2.0-alpha
- **ステータス**: Phase 1-5 完了 + Acknowledgment システム実装済み
- **Node.js**: 20.x LTS
- **作成日**: 2025-12-02
- **最終更新**: 2025-12-04 (Acknowledgment ベース状態同期実装完了)

---

## 主な機能

### ✅ 実装済み

- [x] **ルーム管理**
  - ルーム作成（カスタムブラインド設定）
  - プレイヤー参加/退出
  - ルーム情報取得

- [x] **ゲーム機能**
  - ゲーム開始（2-9人プレイヤー対応）
  - プレイヤーアクション (fold, check, call, raise, allin, acknowledge)
  - ステージ進行 (preflop → flop → turn → river → showdown)
  - ポット管理（メインポット、サイドポット）
  - **Acknowledgment ベース状態同期** 🆕
    - 全クライアントの画面更新を待ってから次のステップへ進行
    - クライアント主導の状態同期で不整合を防止
    - all-in 時も各ステージごとにクライアントの確認を待つ

- [x] **API エンドポイント**
  - REST API (Express)
  - OpenAPI 3.0仕様書
  - Swagger UI統合

- [x] **WebSocket通知** (Phase 4) 🆕
  - **シンプル設計**: 通知のみ送信、詳細はREST APIで取得
  - Socket.io統合
  - 最小限のイベント（room:updated、error）

- [x] **テスト**
  - エンジンテスト (190テスト - acknowledgment 対応)
  - サービス層テスト (35テスト)
  - 統合テスト (48テスト - acknowledgment 対応)
  - 100%パス率（273テスト）

### 📅 計画中

- [ ] **クライアント実装** (Phase 6)
  - OpenAPIからTypeScriptクライアント自動生成
  - React UI コンポーネント

---

## アーキテクチャ

```
┌─────────────────────────────────────────────────────┐
│                    Client Layer                     │
│           (将来実装 - Phase 6)                       │
└─────────────────────────────────────────────────────┘
              ↓ HTTP/REST          ↓ WebSocket
┌─────────────────────────────────────────────────────┐
│              API + WebSocket Layer                  │
│  ┌─────────────────┐    ┌─────────────────┐        │
│  │  /api/v1/rooms  │    │ Socket.io       │        │
│  │   REST API      │    │ WebSocket       │        │
│  └─────────────────┘    └─────────────────┘        │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│              Service Layer (GameService)            │
│  - ビジネスロジック                                   │
│  - Either型エラーハンドリング                         │
│  - WebSocket通知トリガー (Notifier統合) ✅           │
└─────────────────────────────────────────────────────┘
        ↓                                    ↓
┌──────────────────┐              ┌──────────────────┐
│  GameManagerV2   │              │   GameNotifier   │
│  - State管理     │              │  - Socket.io通知 │
└──────────────────┘              └──────────────────┘
        ↓
┌─────────────────────────────────────────────────────┐
│         Engine Layer (純粋関数型エンジン)            │
│  - fp-ts による関数型プログラミング                    │
│  - 副作用のないゲームロジック                          │
│  - 181テスト、96%カバレッジ                           │
└─────────────────────────────────────────────────────┘
```

### ディレクトリ構造

```
server-v2/
├── src/
│   ├── api/
│   │   ├── openapi.yaml          # OpenAPI 3.0仕様書
│   │   └── routes/
│   │       ├── rooms.ts          # ルーム管理API
│   │       └── actions.ts        # ゲームアクションAPI
│   ├── websocket/                # WebSocket層 🆕
│   │   ├── events.ts             # イベント型定義
│   │   ├── notifier.ts           # 通知サービス
│   │   └── handler.ts            # Socket.ioハンドラー
│   ├── services/
│   │   └── GameService.ts        # ビジネスロジック層
│   ├── managers/
│   │   └── GameManager.ts        # 状態管理層
│   ├── types/
│   │   ├── internal.ts           # 内部型定義
│   │   └── pokersolver.d.ts      # pokersolver型定義
│   └── server.ts                 # エントリーポイント
├── tests/
│   ├── helpers/
│   │   └── testServer.ts         # テスト用ヘルパー (acknowledgment対応) 🆕
│   ├── websocket/                # WebSocketテスト 🆕
│   │   └── Notifier.test.ts      # Notifierユニットテスト
│   ├── managers/
│   │   └── GameManager.test.ts   # GameManagerユニットテスト
│   ├── services/
│   │   └── GameService.test.ts   # GameServiceユニットテスト
│   ├── engine/                   # エンジンテスト
│   │   └── *.test.ts             # 各種エンジンテスト
│   └── integration/
│       ├── api-game-flow.test.ts             # 基本統合テスト (ack対応) 🆕
│       ├── api-game-flow-heads-up.test.ts    # ヘッズアップテスト (ack対応) 🆕
│       └── api-websocket-integration.test.ts # WebSocket統合テスト (ack対応) 🆕
├── package.json
├── tsconfig.json
├── jest.config.js
├── README.md                              # このファイル
├── ACKNOWLEDGMENT_SYNC_DESIGN.md          # Acknowledgmentシステム設計書 🆕
├── GAME_FLOW_SEQUENCE.md                  # ゲームフロー詳細仕様 🆕
└── TEST_SCENARIOS.md                      # テストシナリオ一覧
```

---

## セットアップ

### 必須要件

- **Node.js**: 20.x LTS以上
- **npm**: 10.x以上

### インストール

```bash
# リポジトリクローン
git clone <repository-url>
cd ff-poker/server-v2

# 依存関係インストール
npm install
```

### 環境変数（オプション）

```bash
# .env ファイル作成（オプション）
PORT=3001
CORS_ORIGIN=http://localhost:5173
NODE_ENV=development
```

---

## 使い方

### 開発サーバー起動

```bash
npm run dev
```

サーバーが起動すると以下のURLでアクセス可能:
- **API サーバー**: http://localhost:3001
- **Swagger UI**: http://localhost:3001/api-docs
- **Health Check**: http://localhost:3001/health

### 本番ビルド

```bash
# TypeScriptビルド
npm run build

# 本番サーバー起動
npm start
```

### 基本的なAPIの使い方

#### 1. ルーム作成

```bash
curl -X POST http://localhost:3001/api/v1/rooms \
  -H "Content-Type: application/json" \
  -d '{
    "hostName": "Player1",
    "smallBlind": 10,
    "bigBlind": 20
  }'
```

**レスポンス:**
```json
{
  "roomId": "room-abc123",
  "hostId": "player-xyz789",
  "players": [...]
}
```

#### 2. プレイヤー参加

```bash
curl -X POST http://localhost:3001/api/v1/rooms/room-abc123/join \
  -H "Content-Type: application/json" \
  -d '{
    "playerName": "Player2"
  }'
```

#### 3. ゲーム開始

```bash
curl -X POST http://localhost:3001/api/v1/rooms/room-abc123/start
```

#### 4. アクション実行

```bash
curl -X POST http://localhost:3001/api/v1/rooms/room-abc123/actions \
  -H "Content-Type: application/json" \
  -d '{
    "playerId": "player-xyz789",
    "action": {
      "type": "raise",
      "amount": 40
    }
  }'
```

#### 5. ゲーム状態取得

```bash
curl "http://localhost:3001/api/v1/rooms/room-abc123/state?playerId=player-xyz789"
```

#### 6. Acknowledgment 送信 🆕

```bash
# クライアントが画面更新を完了したことを通知
curl -X POST http://localhost:3001/api/v1/rooms/room-abc123/actions \
  -H "Content-Type: application/json" \
  -d '{
    "playerId": "player-xyz789",
    "action": {
      "type": "acknowledge"
    }
  }'
```

**注意**: Acknowledgment アクションは、通常のゲームアクション（fold, call, raise など）の後に、全プレイヤーが送信する必要があります。全員から ack を受信するまで、ゲームは次のステップに進みません。

---

## API仕様

### OpenAPI仕様書

完全なAPI仕様は以下で確認できます:
- **Swagger UI**: http://localhost:3001/api-docs (サーバー起動後)
- **YAML**: [src/api/openapi.yaml](./src/api/openapi.yaml)

### エンドポイント一覧

#### ルーム管理

| Method | Endpoint | 説明 |
|--------|----------|------|
| POST | `/api/v1/rooms` | ルーム作成 |
| GET | `/api/v1/rooms/:roomId` | ルーム情報取得 |
| POST | `/api/v1/rooms/:roomId/join` | ルーム参加 |

#### ゲーム操作

| Method | Endpoint | 説明 |
|--------|----------|------|
| POST | `/api/v1/rooms/:roomId/start` | ゲーム開始 |
| POST | `/api/v1/rooms/:roomId/actions` | アクション実行 |
| GET | `/api/v1/rooms/:roomId/state` | ゲーム状態取得 |

#### その他

| Method | Endpoint | 説明 |
|--------|----------|------|
| GET | `/health` | ヘルスチェック |
| GET | `/api/v1` | APIバージョン情報 |
| GET | `/api-docs` | Swagger UI |

### プレイヤーアクションタイプ

```typescript
type ActionType = 'fold' | 'check' | 'call' | 'raise' | 'allin' | 'acknowledge';
```

**`acknowledge` アクション** 🆕: クライアントが画面更新を完了したことをサーバーに通知するための特殊アクション。全プレイヤーから acknowledge を受信するまで、次のステップへ進まない。

### ゲームステージ

```typescript
type Stage = 'preflop' | 'flop' | 'turn' | 'river' | 'showdown' | 'ended';
```

---

## Acknowledgment システム 🆕

### 概要

Acknowledgment システムは、クライアント・サーバー間の状態同期を確実にし、クライアント側の画面遷移制御を簡素化するための仕組みです。

### 動作フロー

```
┌─────────────┐
│ Client A/B/C│
└──────┬──────┘
       │ (1) Action (fold/call/raise)
       ↓
┌─────────────┐
│   Server    │ (2) processAction()
│ GameService │     ↓
└──────┬──────┘     GameState (waitingForAck: true)
       │
       │ (3) Broadcast: room:updated
       ├────────────┐
       │            │
       ↓            ↓
┌──────────┐  ┌──────────┐
│ Client A │  │ Client B │
│ 画面更新 │  │ 画面更新 │
└────┬─────┘  └────┬─────┘
     │             │
     │ (4) ack     │ (4) ack
     └─────┬───────┘
           ↓
     ┌─────────────┐
     │   Server    │ (5) 全ack受信確認
     │ GameService │     ↓
     └──────┬──────┘     resolveAcknowledgment()
            │            ↓
            │            次のステップへ (必要なら)
            │
            │ (6) Broadcast: room:updated (次の状態)
            ↓
```

### 主要な機能

#### 1. アクション後の ack 待ち状態

```typescript
// GameState に追加されたフィールド
interface GameState {
  // ... 既存のフィールド
  readonly waitingForAck: boolean;  // ack 待ちフラグ
  readonly ackState: Option<AcknowledgmentState>;  // ack 状態
}
```

#### 2. 状態遷移パターン

**パターンA: 通常のアクション**
```
stage: preflop, waitingForAck: false (Player1 の番)
  ↓ Player1: call
stage: preflop, waitingForAck: true
  ↓ 全プレイヤーから ack
stage: preflop, waitingForAck: false (Player2 の番)
```

**パターンB: all-in による自動進行**
```
stage: preflop, waitingForAck: false (全員 all-in)
  ↓ 最後の call アクション
stage: preflop, waitingForAck: true
  ↓ 全プレイヤーから ack
stage: flop, waitingForAck: true  ← 自動進行、但し ack 待ち
  ↓ 全プレイヤーから ack
stage: turn, waitingForAck: true  ← 次のステージへ
```

### メリット

- ✅ **確実な状態同期**: 全クライアントが画面更新を完了してから次のステップへ
- ✅ **クライアント側の簡素化**: 複雑な状態管理が不要、画面更新 → ack のシンプルなフロー
- ✅ **デバッグの容易性**: どのクライアントが ack を返していないかが明確
- ✅ **段階的な進行**: all-in でも各ステージごとに画面更新を待つ

### 使用例

#### クライアント側の実装

```typescript
// room:updated イベントを受信
socket.on('room:updated', async (data) => {
  // 1. 最新の状態を取得
  const state = await fetchRoomState(roomId, playerId);

  // 2. 画面を更新
  updateUI(state);

  // 3. ack を送信
  await sendAction({
    playerId,
    type: 'acknowledge',
    acknowledgedAt: Date.now(),
  });
});
```

### 詳細仕様

詳細な設計仕様は [ACKNOWLEDGMENT_SYNC_DESIGN.md](./ACKNOWLEDGMENT_SYNC_DESIGN.md) を参照してください。

---

### クライアントコード生成（未実装）

**現状**: OpenAPI定義のみ作成済み

**Phase 6で実装予定**:
- OpenAPI GeneratorによるTypeScriptクライアント自動生成
- 型安全なAPIクライアント
- React hooks統合

```bash
# 将来的な使用例（Phase 6）
npm run generate:client
```

---

## 開発

### スクリプト

```bash
# 開発サーバー（ホットリロード）
npm run dev

# ビルド
npm run build

# 本番起動
npm start

# 型チェック
npm run type-check

# リント
npm run lint
```

### テスト

```bash
# 全テスト実行
npm test

# ウォッチモード
npm run test:watch

# カバレッジレポート
npm run test:coverage

# 特定のテストスイート
npm test -- --testPathPattern="GameManager"
npm test -- --testPathPattern="heads-up"
```

### テスト結果

```
✅ Engine tests:                    190/190 passed (acknowledgment 対応)
✅ GameManager.test.ts:              17/17 passed
✅ GameService.test.ts:               10/10 passed
✅ Notifier.test.ts:                   8/8 passed
✅ api-game-flow.test.ts:             13/13 passed (acknowledgment 対応)
✅ api-game-flow-heads-up.test.ts:    33/33 passed (acknowledgment 対応)
✅ api-websocket-integration.test.ts:  2/2 passed (acknowledgment 対応)

合計: 273/273 passed (100%) 🎉
```

詳細なテストシナリオは [TEST_SCENARIOS.md](./TEST_SCENARIOS.md) を参照。

### コーディング規約

プロジェクト全体の規約は [../.claude/CLAUDE.md](../.claude/CLAUDE.md) を参照。

**主要な規約:**
- TypeScript厳格モード
- `any`型禁止（`unknown`を使用）
- 関数の戻り値型を明示
- ESLint/Prettierによる自動フォーマット
- Conventional Commitsでのコミットメッセージ

---

## トラブルシューティング

### よくある問題

#### 1. ポート3001が既に使用されている

```bash
# プロセス確認
lsof -i :3001

# プロセス停止
kill -9 <PID>

# または別のポートを使用
PORT=3002 npm run dev
```

#### 2. テストが失敗する

```bash
# キャッシュクリア
npm test -- --clearCache

# node_modules再インストール
rm -rf node_modules package-lock.json
npm install
```

#### 3. 型エラーが出る

```bash
# 型チェック実行
npm run type-check

# engine層の型定義を確認
# server-v2は../server/src/engine/を参照
```

### デバッグ

```bash
# サーバーログ確認（開発モード）
npm run dev

# 特定のテストをデバッグ
npm test -- --testPathPattern="GameService" --verbose
```

---

## ロードマップ

### Phase 1-5: 完了 ✅
- [x] OpenAPI定義とプロジェクトセットアップ
- [x] GameManagerV2とGameService実装
- [x] REST APIエンドポイント実装
- [x] HTTP統合テスト作成
- [x] WebSocketサーバー実装
- [x] リアルタイム通知機能
- [x] Socket.io統合
- [x] **Acknowledgment ベース状態同期システム** 🆕
  - [x] エンジン層実装（フェーズ1）
  - [x] サーバー層実装（フェーズ2）
  - [x] API層対応
  - [x] 統合テスト更新（273テスト通過）

### Phase 6: 将来計画 📅
- [ ] **Acknowledgment システム - クライアント実装** 🎯
  - [ ] WebSocket による自動 acknowledge 送信
  - [ ] 画面更新完了の検知
  - [ ] リトライロジック
- [ ] TypeScriptクライアント自動生成
- [ ] React UIコンポーネント
- [ ] WebSocketクライアント実装

### 追加機能
- [ ] Redis セッション管理
- [ ] 再接続ロジック
- [ ] ロギング・モニタリング
- [ ] E2Eテスト（Playwright）

---

## 依存関係

### 本番依存

```json
{
  "express": "^4.18.2",
  "fp-ts": "^2.16.0",
  "socket.io": "^4.6.1",
  "swagger-ui-express": "^5.0.0",
  "uuid": "^9.0.0",
  "yaml": "^2.3.4",
  "cors": "^2.8.5"
}
```

### 開発依存

```json
{
  "typescript": "^5.0.0",
  "jest": "^29.7.0",
  "supertest": "^6.3.3",
  "ts-jest": "^29.1.0",
  "ts-node-dev": "^2.0.0"
}
```

---

## 貢献

このプロジェクトはモノレポ構成です。

### ブランチ戦略

```
main (本番)
  └─ develop (開発)
       ├─ feature/機能名
       ├─ fix/バグ名
       └─ refactor/対象
```

### コミットメッセージ

Conventional Commits を使用:

```
feat(api): Add WebSocket notification support
fix(game): Fix side pot calculation
docs(readme): Update API documentation
test(integration): Add heads-up game scenarios
```

---

## 関連ドキュメント

- [ACKNOWLEDGMENT_SYNC_DESIGN.md](./ACKNOWLEDGMENT_SYNC_DESIGN.md) - Acknowledgment システム設計書 🆕
- [GAME_FLOW_SEQUENCE.md](./GAME_FLOW_SEQUENCE.md) - ゲームフロー詳細仕様 🆕
- [TEST_SCENARIOS.md](./TEST_SCENARIOS.md) - テストシナリオ一覧
- [src/engine/README.md](./src/engine/README.md) - エンジン仕様書
- [../.claude/CLAUDE.md](../.claude/CLAUDE.md) - プロジェクトガイドライン

---

## ライセンス

MIT

---

## サポート

問題が発生した場合:
1. [ACKNOWLEDGMENT_SYNC_DESIGN.md](./ACKNOWLEDGMENT_SYNC_DESIGN.md) で設計仕様を確認
2. [TEST_SCENARIOS.md](./TEST_SCENARIOS.md) でテストシナリオを確認
3. Issueを作成

---

**最終更新**: 2025-12-04
**メンテナ**: Claude Code
**バージョン**: 2.2.0-alpha
**Acknowledgment システム実装完了**: 2025-12-04
